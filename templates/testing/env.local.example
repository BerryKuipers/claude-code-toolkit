# Copy this to .env.local and fill in your values
# .env.local is git-ignored and will NOT be committed

# Test User Credentials (for automated testing)
# Used by scripts/get-test-tokens.mjs to login and get auth tokens
TEST_USER_EMAIL=tester@wescobar.org
TEST_USER_PASSWORD=your_password_here

# API URL (optional, defaults to http://localhost:3000)
#VITE_API_URL=http://localhost:3000
